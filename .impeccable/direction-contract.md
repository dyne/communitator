# Communitator documentation direction

FORM seed key: `9aa01686`

## World

Broadcast Control Log, grounded in community-radio control rooms and paper station logs. Console black, warm paper, signal orange, green, and gold form the palette; engraved control labels, signal lamps, routing linework, and log tables form the material language.

## First viewport

A leader reads the plain proposition beside a live routing console. Kinds `10002`, `10063`, and `10050` converge through a named NIP-07 signing point, then branch to community and blast relays. On narrow screens the same route becomes a vertically labeled signal chain rather than a scaled-down diagram.

## Visitor path

The page moves from route definition to link generation, member review, signing, publishing, event reference, and a final leader-guide action. Documentation navigation continues the station-log vocabulary without changing the information hierarchy.

## Signature interaction

One dashed signal pulse travels along the desktop route. It is the only authored motion and collapses to a still, labeled route under reduced-motion preferences and on the narrow composition.

## Cross-surface reach and risk

The system extends to guide headings, channel labels, event tables, focus states, search, sidebars, and the Dyne footer. Its honest risk is decorative audio cosplay; every control-room element must carry real product structure or verified protocol information.

## Quality card

The selected grounded card is `model-pick` in `.impeccable/direction-options.json`. It is a code-led direction card with no generated or catalog comp.
